export const ModelsTags = {
	name: "Models",
	description:
		"Endpoints for managing models, entities whose `data` is a JSON payload validated against the schema defined by their associated `ModelsType`. Use these to create, read, update, delete, and list models, as well as to query them by their `ModelsType`.",
} as const;
